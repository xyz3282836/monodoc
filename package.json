{
  "name": "monodoc",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "note:build": "pnpm --filter=note docs:build",
    "note:clean-dev": "pnpm --filter=note docs:clean-dev",
    "note:dev": "pnpm --filter=note docs:dev",
    "blog:build": "pnpm --filter=blog docs:build",
    "blog:clean-dev": "pnpm --filter=blog docs:clean-dev",
    "blog:dev": "pnpm --filter=blog docs:dev",
    "docs:mv": "pnpm --filter=note docs:build && cp -r ./packages/note/src/.vuepress/dist/ ../note && cd ../note && git add ./* && git commit -m 'update doc' && git push"
  },
  "dependencies": {

  },
  "devDependencies": {

  },
  "license": "ISC"
}