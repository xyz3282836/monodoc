{
  "name": "monodoc",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "note:new": "rm -fr ./packages/note/src/raw/ && cp -r /Users/zhou/Documents/我的坚果云/读书笔记/ ./packages/note/src/raw/",
    
    "acp": "git add ./ && git commit -m 'update doc' && git push",
    
    "note:build": "pnpm --filter=note docs:build && cp -r ./packages/note/src/.vuepress/dist/ ../note && cd ../note && git pull && git add ./ && git commit -m 'update doc' && git push",
    "blog:build": "pnpm --filter=blog build:vite && cp -r ./packages/blog/src/.vuepress/dist/ ../blog && cd ../blog && git pull && git add ./ && git commit -m 'update doc' && git push",
    
    "note:clean-dev": "pnpm --filter=note docs:clean-dev",
    "note:dev": "pnpm --filter=note docs:dev",
    "note:update-package": "pnpm --filter=note docs:update-package",
    
    "blog:dev": "pnpm --filter=blog dev:vite"
  },
  "dependencies": {},
  "devDependencies": {},
  "license": "ISC"
}